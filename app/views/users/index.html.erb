<h1>List of Users</h1>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.username, user_path(user) %>
      <%= user.votes.count %>
      <%= user.joined %>
    </li>
  <% end %>
</ul>

<%= link_to "Back to Media List", root_path %>
